{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class=" offset-md-3 col-md-6">
            <h2 class="mb-3">Add your boat</h2>
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <a href="{{ path('boat_index') }}" class="col-md-4 offset-md-2">
                    <i class="fas fa-chevron-left"></i> Back to the list
                </a>
                <button class="btn btn-primary btn-lg col-md-4" type="submit">Add Boat</button>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
